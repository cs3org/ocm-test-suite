name: OCM Test Code flow Nextcloud v33 to Cernbox v2

on:
  workflow_dispatch:

jobs:
  call-test-engine:
    uses: ./.github/workflows/tests-engine.yml
    with:
      scenario:          code-flow
      sender_platform:   nextcloud
      sender_version:    v33
      receiver_platform: cernbox
      receiver_version:  v2
